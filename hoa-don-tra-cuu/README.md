
📄 Nội dung mẫu:

```markdown
# Dự án tra cứu hóa đơn tự động với Python Selenium

Nhập mã trong `input/ma_tra_cuu.txt`, chạy `main.py` để tra cứu hóa đơn.

Không gán mã hóa đơn cứng trong code.
Không đẩy file PDF, XML lên GitHub (đã có .gitignore).
